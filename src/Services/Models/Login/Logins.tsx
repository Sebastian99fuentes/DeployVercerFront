export type UserProfileToken = {
    userId: string;
    userName: string;
    email: string;
    token: string;
    id: string;
} 

export type UserProfile = {
    userName: string;
    email: string;
}